# Neon PostgreSQL — keep secret, never commit .env.local
# Use as DATABASE_URL for @neondatabase/serverless (raw SQL)
DATABASE_URL=postgresql://user:password@ep-xxx-pooler.c-7.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Vercel deploy token (local only)
VERCEL_TOKEN=your_vercel_token_here
